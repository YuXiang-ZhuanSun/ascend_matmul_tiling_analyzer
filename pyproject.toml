[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "matmul-tiling-replay"
version = "0.1.0"
description = "MatMulV3 tiling-key replay and source-trace analyzer"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
